<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>
    <dimen name="fab_margin">16dp</dimen>
    <dimen name="design_bottom_navigation_text_size"
        tools:override="true">11sp </dimen>

    <dimen name="design_bottom_navigation_active_text_size"
        tools:override="true">12sp </dimen>

    <!-- Text Sizes go in the dimens.xml file (sp, dp, px, etc) -->

    <dimen name="add_fence_button_width">60dp</dimen>
    <dimen name="add_fence_button_height">40dp</dimen>

    <dimen name="add_friend_button_width">60dp</dimen>
    <dimen name="add_friend_button_height">40dp</dimen>

    <!-- Text Sizes for information details UI (sp units) -->
    <dimen name="info_small_text_font">6sp</dimen>
    <dimen name="info_labels_text_font">8sp</dimen>
    <dimen name="title_text_font">8sp</dimen>

    <!-- Text Sizes for friends list UI (sp units) -->
    <dimen name="name_text_font">24sp</dimen>
    <dimen name="time_text_font">20sp</dimen>
    <dimen name="battery_lvl_text_font">12sp</dimen>

    <!-- Text Sizes for friends list titlebar (sp units) -->
    <dimen name="friends_title_font">24sp</dimen>
    <dimen name="friends_add_friend_font">30sp</dimen>

    <!-- Text Sizes for fences list titlebar (sp units) -->
    <dimen name="fences_title_font">24sp</dimen>
    <dimen name="fences_add_fence_font">30sp</dimen>
    <dimen name="fences_edit">24sp</dimen>
    <dimen name="fences_done">24sp</dimen>

    <!-- Text Sizes for notifications list titlebar (sp units) -->
    <dimen name="notifications_title_font">24sp</dimen>
    <dimen name="notifications_clear_title_font">24sp</dimen>

    <!-- Bottom Navigation Notification Badge Text Size (sp units) -->
    <dimen name="bottom_navigation_notification_badge_font">1px</dimen>

    <!-- LBAs list item text size (sp units) -->
    <dimen name="lba_text_size">8sp</dimen>

    <!-- LBAs list lba status text size (sp units) -->
    <dimen name="lba_status_size">8sp</dimen>

    <!-- Select Fence text size (sp units) -->
    <dimen name="select_fence_text_size">25sp</dimen>

    <!-- view_fence border stroke width size (px) -->
    <dimen name="view_fence_border_width">1px</dimen>

    <!-- edit_fence border stroke width size (px) -->
    <dimen name="edit_fence_border_width">1px</dimen>

    <!-- add_fence border stroke width size (px) -->
    <dimen name="add_fence_border_width">1px</dimen>

    <!-- friend's movement border stroke width size (px) -->
    <dimen name="friends_movement_border_width">1px</dimen>

    <!-- Default border_background stroke width (px) -->
    <dimen name="border_background_stroke_width">2px</dimen>

    <!-- View Fence title size (sp) -->
    <dimen name="view_fence_title">10sp</dimen>

    <!-- Edit Fence title size (sp) -->
    <dimen name="edit_fence_title">10sp</dimen>

    <!-- Information Icon height -->
    <dimen name="info_height">38dp</dimen>

    <!-- Information Icon width -->
    <dimen name="info_width">38dp</dimen>

    <!-- Login Dialog Title size -->
    <dimen name="login_dialog_size">12dp</dimen>

    <!-- Badge count text size in sp -->
    <dimen name="notification_badge_count_text_size">5sp</dimen>

</resources>


